---
// One dashboard only: the old internal wizard confused the flow next to the
// client dashboard, so /studio now lands on /dashboard (decided on demo day).
return Astro.redirect('/dashboard', 302);
---
